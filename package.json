{
  "name": "pharmacy-mgt",
  "version": "1.0.0",
  "description": "application for pharmacy management",
  "main": "server.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/server.ts",
    "build": "rm -rf build/ && prettier --write source/ && tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@loopback/typeorm": "^0.9.5",
    "express": "^4.18.2",
    "nodemon": "^3.0.3",
    "reflect-metadata": "^0.2.1",
    "sqlite3": "^5.1.7",
    "ts-node": "^10.9.2",
    "typeorm": "^0.3.20",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
